// #include <stdio.h>
// #include <stdlib.h>
// #include "../headers/utility.h"

// // Utility Functions
// size_t fpread(void *buffer, size_t size, size_t mitems, size_t offset, FILE *stream)
// {
//     if( (fseek(stream, offset, SEEK_SET)) != 0 )
//     {
//         return 0;
//     }
//     return fread(buffer, size, mitems, stream);
// } 
